// Public API for the gas-forecast feature.
// Consumers must import the workspace through this boundary rather than its legacy location.
export { GasForecastWorkspace } from "@/components/observatory/gas-forecast-workspace";
