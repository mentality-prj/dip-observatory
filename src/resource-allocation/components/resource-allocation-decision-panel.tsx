"use client";

import { useEffect, useState } from "react";
import { Check, CircleAlert, Flag, Gauge, Save, X } from "lucide-react";
import type { Locale } from "@/lib/observatory-i18n";

type CapacityRecommendation = { resource: string; extra_team_equivalents: number; added_capacity: number; priority_coverage: number; delta_priority_coverage: number; marginal_gain: number; diminishing_returns: boolean; target_reached: boolean };
type CapacityBottleneck = { resource: string; priority_demand: number; available_capacity: number; capacity_shortfall: number; first_increment_gain: number; binding: boolean };
type CapacityGap = { status: "ok" | "infeasible"; operation: "capacity_gap"; current?: { priority_coverage?: number }; target_priority_coverage?: number; gap_to_target?: number; target_status?: "already_met" | "gap"; minimum_capacity_to_target?: CapacityRecommendation[]; bottlenecks?: CapacityBottleneck[]; marginal_scenarios?: CapacityRecommendation[] };
type Lifecycle = { decisionId: string; status: string } | null;
type Props = { input: Record<string, unknown>; selected: Record<string, unknown>; manualSelected?: Record<string, unknown> | null; priorityCoverage: number; served: number; unmet: number; locale: Locale };
const copy = {
  uk: { capacity: "ДЕФІЦИТ РЕСУРСІВ", capacityTitle: "Яких ресурсів бракує для кращого результату?", analyze: "Розрахувати потребу для 90% пріоритетного покриття", analyzing: "Розрахунок…", infeasible: "Для поточної операційної ситуації неможливо виконати аналіз доступної потужності.", gap: "До цілі бракує", status: "Стан", reached: "Ціль досягнута", gapStatus: "Є дефіцит", already: "Поточне покриття пріоритетних потреб уже досягає 90%. Додаткові ресурси не потрібні.", notEnough: "Навіть протестоване додавання до трьох еквівалентів команди на кожну послугу не забезпечує 90%. Нижче показані обмеження, які стримують результат.", additions: "Мінімальні протестовані додаткові ресурси для досягнення цілі", teamEq: "екв. команди", capacityUnit: "потужності", priority: "Пріоритетне покриття", improvement: "покращення", bottlenecks: "Критичні дефіцити за послугами", demand: "пріоритетний попит", available: "доступна потужність", shortfall: "дефіцит", firstTeam: "від першого екв. команди", decision: "РІШЕННЯ МЕНЕДЖЕРА", decisionTitle: "Рекомендація → рішення → фактичний результат", staged: "Перевірений ручний план підготовлено як рішення менеджера.", snapshot: "Створити незмінний знімок рішення", snapshotBusy: "Створення знімка…", decisionId: "ID рішення", reason: "Обґрунтування зміни або відхилення", accept: "Прийняти план DIP", modify: "Прийняти ручне коригування", alternative: "Вибрати альтернативу", reject: "Відхилити", outcomeNotes: "Що відбулося фактично після виконання рішення", record: "Зафіксувати фактичний результат", recording: "Збереження…", completed: "Рішення завершено.", completedText: "Рекомендація DIP, дія менеджера та фактичний результат пов’язані в одному життєвому циклі рішення." },
  en: { capacity: "CAPACITY GAP", capacityTitle: "What additional capacity would improve the result?", analyze: "Analyze capacity needed for 90% priority coverage", analyzing: "Analyzing…", infeasible: "Capacity analysis is infeasible for the current operational state.", gap: "Gap to target", status: "Status", reached: "Target reached", gapStatus: "Capacity gap", already: "Current priority coverage already meets the 90% target. No additional capacity is required.", notEnough: "The tested additions of up to three team-equivalents per service do not reach 90%. Review the binding constraints below.", additions: "Minimum tested additions that reach the target", teamEq: "team eq.", capacityUnit: "capacity", priority: "Priority coverage", improvement: "improvement", bottlenecks: "Binding service bottlenecks", demand: "priority demand", available: "available capacity", shortfall: "shortfall", firstTeam: "from first team eq.", decision: "HUMAN DECISION", decisionTitle: "Recommendation → decision → actual outcome", staged: "A feasible manager-edited plan is staged as the manager decision.", snapshot: "Create immutable decision snapshot", snapshotBusy: "Creating snapshot…", decisionId: "Decision ID", reason: "Reason for modification or rejection", accept: "Accept DIP plan", modify: "Approve manual modification", alternative: "Use selected alternative", reject: "Reject", outcomeNotes: "What actually happened after the decision was executed", record: "Record actual outcome", recording: "Recording…", completed: "Decision completed.", completedText: "The DIP recommendation, manager action and actual outcome are linked in one decision lifecycle." },
  pl: { capacity: "DEFICYT ZASOBÓW", capacityTitle: "Jakich zasobów brakuje, aby poprawić wynik?", analyze: "Oblicz potrzeby dla 90% pokrycia priorytetów", analyzing: "Analiza…", infeasible: "Dla bieżącej sytuacji operacyjnej nie można wykonać analizy dostępnej zdolności.", gap: "Brak do celu", status: "Stan", reached: "Cel osiągnięty", gapStatus: "Deficyt zasobów", already: "Bieżące pokrycie potrzeb priorytetowych osiąga już 90%. Dodatkowe zasoby nie są wymagane.", notEnough: "Nawet testowane dodanie do trzech ekwiwalentów zespołu na usługę nie zapewnia 90%. Poniżej pokazano ograniczenia blokujące wynik.", additions: "Minimalne testowane dodatkowe zasoby potrzebne do osiągnięcia celu", teamEq: "ekw. zespołu", capacityUnit: "zdolności", priority: "Pokrycie priorytetów", improvement: "poprawa", bottlenecks: "Krytyczne deficyty według usług", demand: "popyt priorytetowy", available: "dostępna zdolność", shortfall: "deficyt", firstTeam: "od pierwszego ekw. zespołu", decision: "DECYZJA MENEDŻERA", decisionTitle: "Rekomendacja → decyzja → wynik rzeczywisty", staged: "Zweryfikowany plan ręczny przygotowano jako decyzję menedżera.", snapshot: "Utwórz niezmienny zapis decyzji", snapshotBusy: "Tworzenie zapisu…", decisionId: "ID decyzji", reason: "Uzasadnienie zmiany lub odrzucenia", accept: "Zaakceptuj plan DIP", modify: "Zaakceptuj korektę ręczną", alternative: "Wybierz alternatywę", reject: "Odrzuć", outcomeNotes: "Co faktycznie wydarzyło się po wykonaniu decyzji", record: "Zapisz rzeczywisty wynik", recording: "Zapisywanie…", completed: "Decyzja zakończona.", completedText: "Rekomendacja DIP, działanie menedżera i rzeczywisty wynik są połączone w jednym cyklu życia decyzji." },
} as const;
async function post(path: string, body: Record<string, unknown>) { const response = await fetch(path, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const payload = await response.json(); if (!response.ok) { const detail = typeof payload.detail === "string" ? payload.detail : payload.detail?.message; throw new Error(payload.error ?? detail ?? `Request failed (${response.status})`); } return payload; }

export function ResourceAllocationDecisionPanel({ input, selected, manualSelected, priorityCoverage, served, unmet, locale }: Props) {
  const t = copy[locale]; const [capacity, setCapacity] = useState<CapacityGap | null>(null); const [lifecycle, setLifecycle] = useState<Lifecycle>(null); const [reason, setReason] = useState(""); const [notes, setNotes] = useState(""); const [busy, setBusy] = useState<string | null>(null); const [error, setError] = useState<string | null>(null);
  useEffect(() => { setLifecycle(null); setCapacity(null); setReason(""); setNotes(""); }, [input]);
  async function capacityGap() { setBusy("capacity"); setError(null); try { setCapacity(await post("/api/resource-allocation/capacity-gap", { ...input, target_priority_coverage: 0.9 })); } catch (e) { setError(e instanceof Error ? e.message : "Capacity analysis failed"); } finally { setBusy(null); } }
  async function persist() { setBusy("persist"); setError(null); try { const payload = await post("/api/resource-allocation/decisions", input); setLifecycle({ decisionId: payload.decision_id, status: payload.status }); } catch (e) { setError(e instanceof Error ? e.message : "Decision persistence failed"); } finally { setBusy(null); } }
  async function feedback(status: "accepted" | "modified" | "rejected") { if (!lifecycle) return; if ((status === "modified" || status === "rejected") && !reason.trim()) { setError(t.reason); return; } setBusy(status); setError(null); try { const body: Record<string, unknown> = { status }; if (status === "modified") { body.reason = reason.trim(); body.selected = manualSelected ?? selected; } if (status === "rejected") body.reason = reason.trim(); const payload = await post(`/api/resource-allocation/decisions/${encodeURIComponent(lifecycle.decisionId)}/feedback`, body); setLifecycle({ ...lifecycle, status: payload.status }); } catch (e) { setError(e instanceof Error ? e.message : "Decision update failed"); } finally { setBusy(null); } }
  async function outcome() { if (!lifecycle) return; setBusy("outcome"); setError(null); try { const payload = await post(`/api/resource-allocation/decisions/${encodeURIComponent(lifecycle.decisionId)}/outcomes`, { actual_allocation: manualSelected ?? selected, metrics: { priority_coverage: priorityCoverage, served, closing_unmet: unmet }, notes: notes || undefined }); setLifecycle({ ...lifecycle, status: payload.status }); } catch (e) { setError(e instanceof Error ? e.message : "Outcome recording failed"); } finally { setBusy(null); } }
  const recommendations = capacity?.minimum_capacity_to_target ?? []; const bindingBottlenecks = capacity?.bottlenecks?.filter((item) => item.binding) ?? [];
  return <div className="grid gap-5 xl:grid-cols-2"><section className="bg-[#191919] p-6 text-white"><div className="flex items-center justify-between gap-3"><div><div className="text-xs font-bold uppercase tracking-wider text-red-400">08 · {t.capacity}</div><h3 className="mt-2 text-xl font-black">{t.capacityTitle}</h3></div><Gauge className="h-6 w-6"/></div><button disabled={Boolean(busy)} onClick={capacityGap} className="mt-5 border border-white/25 px-4 py-3 text-sm font-bold disabled:opacity-40">{busy === "capacity" ? t.analyzing : t.analyze}</button>{capacity && <div className="mt-5 space-y-4 text-sm">{capacity.status === "infeasible" ? <div className="border border-red-400/40 p-4 text-red-200">{t.infeasible}</div> : <><div className="grid grid-cols-2 gap-3"><div className="border border-white/15 p-3"><span className="text-white/50">{t.gap}</span><b className="mt-1 block text-xl">{Math.round((capacity.gap_to_target ?? 0) * 100)} pp</b></div><div className="border border-white/15 p-3"><span className="text-white/50">{t.status}</span><b className="mt-1 block text-xl">{capacity.target_status === "already_met" ? t.reached : t.gapStatus}</b></div></div>{capacity.target_status === "already_met" && <div className="border-l-2 border-emerald-400 pl-3">{t.already}</div>}{capacity.target_status === "gap" && recommendations.length === 0 && <div className="border-l-2 border-amber-300 pl-3">{t.notEnough}</div>}{recommendations.length > 0 && <div><div className="mb-2 text-xs font-bold uppercase tracking-wider text-white/45">{t.additions}</div><div className="space-y-2">{recommendations.map((item) => <div key={`${item.resource}-${item.added_capacity}`} className="border border-white/15 p-3"><div className="flex flex-wrap items-center justify-between gap-2"><b>{item.resource}</b><span>+{item.extra_team_equivalents} {t.teamEq} · +{item.added_capacity.toFixed(0)} {t.capacityUnit}</span></div><div className="mt-1 text-xs text-white/50">{t.priority} {Math.round(item.priority_coverage * 100)}% · {t.improvement} +{(item.delta_priority_coverage * 100).toFixed(1)} pp</div></div>)}</div></div>}{bindingBottlenecks.length > 0 && <div><div className="mb-2 text-xs font-bold uppercase tracking-wider text-white/45">{t.bottlenecks}</div>{bindingBottlenecks.slice(0, 4).map((item) => <div key={item.resource} className="grid grid-cols-[1fr_auto] gap-3 border-t border-white/10 py-2"><span>{item.resource}<span className="block text-xs text-white/45">{t.demand} {item.priority_demand.toFixed(0)} · {t.available} {item.available_capacity.toFixed(0)}</span></span><b className="text-right">{t.shortfall} {item.capacity_shortfall.toFixed(0)}<span className="block text-xs font-normal text-white/45">+{(item.first_increment_gain * 100).toFixed(1)} pp {t.firstTeam}</span></b></div>)}</div>}</>}</div>}</section><section className="bg-white p-6"><div className="text-xs font-bold uppercase tracking-wider text-[#d5222a]">09 · {t.decision}</div><div className="mt-2 flex flex-wrap items-center justify-between gap-3"><h3 className="text-xl font-black">{t.decisionTitle}</h3>{lifecycle && <span className="border border-black/10 px-3 py-1 text-xs font-bold uppercase">{lifecycle.status}</span>}</div>{manualSelected && !lifecycle && <div className="mt-4 border-l-2 border-[#d5222a] pl-3 text-sm">{t.staged}</div>}{!lifecycle ? <button disabled={Boolean(busy)} onClick={persist} className="mt-5 flex items-center gap-2 bg-[#191919] px-4 py-3 text-sm font-bold text-white disabled:opacity-40"><Save className="h-4 w-4"/>{busy === "persist" ? t.snapshotBusy : t.snapshot}</button> : <><div className="mt-4 text-xs text-black/45">{t.decisionId} · {lifecycle.decisionId}</div>{lifecycle.status === "proposed" && <><textarea value={reason} onChange={(event) => setReason(event.target.value)} placeholder={t.reason} className="mt-4 min-h-20 w-full border border-black/15 p-3 text-sm"/><div className="mt-3 flex flex-wrap gap-2"><button onClick={() => feedback("accepted")} disabled={Boolean(busy)} className="flex items-center gap-2 bg-[#191919] px-4 py-3 text-sm font-bold text-white"><Check className="h-4 w-4"/>{t.accept}</button><button onClick={() => feedback("modified")} disabled={Boolean(busy)} className="border border-black/20 px-4 py-3 text-sm font-bold">{manualSelected ? t.modify : t.alternative}</button><button onClick={() => feedback("rejected")} disabled={Boolean(busy)} className="flex items-center gap-2 border border-red-300 px-4 py-3 text-sm font-bold text-red-700"><X className="h-4 w-4"/>{t.reject}</button></div></>}{["accepted", "modified", "rejected"].includes(lifecycle.status) && <><textarea value={notes} onChange={(event) => setNotes(event.target.value)} placeholder={t.outcomeNotes} className="mt-4 min-h-20 w-full border border-black/15 p-3 text-sm"/><button onClick={outcome} disabled={Boolean(busy)} className="mt-3 flex items-center gap-2 bg-[#d5222a] px-4 py-3 text-sm font-bold text-white"><Flag className="h-4 w-4"/>{busy === "outcome" ? t.recording : t.record}</button></>}{lifecycle.status === "completed" && <div className="mt-5 border-l-2 border-green-700 pl-3 text-sm"><b>{t.completed}</b><div className="text-black/50">{t.completedText}</div></div>}</>}{error && <div className="mt-4 border border-red-200 bg-red-50 p-3 text-sm text-red-700"><CircleAlert className="mr-2 inline h-4 w-4"/>{error}</div>}</section></div>;
}
