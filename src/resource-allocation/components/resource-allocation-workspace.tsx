"use client";

import { useState } from "react";
import { ArrowRight, Check, CircleAlert, Play, RotateCcw, Users, X } from "lucide-react";
import type { Locale } from "@/lib/observatory-i18n";

const DEMO = {
  planning_period: { id: "synthetic-week-01", label: "Демонстраційний тиждень" },
  communities: [
    { id: "Громада А", region: "Схід · synthetic", accessible: true, max_teams: 2, demand: [{ service: "psychologist", units: 8, priority: "critical" as const }, { service: "social-worker", units: 5, priority: "high" as const }] },
    { id: "Громада Б", region: "Схід · synthetic", accessible: true, max_teams: 2, demand: [{ service: "legal", units: 6, priority: "high" as const }, { service: "social-worker", units: 4, priority: "normal" as const }] },
    { id: "Громада В", region: "Схід · synthetic", accessible: false, max_teams: 1, demand: [{ service: "psychologist", units: 5, priority: "high" as const }] },
  ],
  teams: [
    { id: "Команда 1", current_community: "Громада Б", skills: ["psychologist", "social-worker"], capacity: 10 },
    { id: "Команда 2", current_community: "Громада А", skills: ["legal", "social-worker"], capacity: 8 },
    { id: "Команда 3", current_community: "Громада Б", skills: ["psychologist"], capacity: 5, allowed_communities: ["Громада А", "Громада Б"] },
  ],
  travel_edges: [{ from: "Громада Б", to: "Громада А", cost: 4 }, { from: "Громада А", to: "Громада Б", cost: 4 }],
  current_allocation: { "Команда 1": "Громада Б", "Команда 2": "Громада А", "Команда 3": "Громада Б" }, synthetic: true,
};
type Metrics = { priority_coverage: number; total_coverage: number; unmet_need: number; capacity_utilization: number; travel_cost: number };
type Alternative = { assignments: Record<string, string | null>; score: number; metrics: Metrics; binding_constraints: string[]; evidence: string[] };
type Result = { status: string; recommended: Alternative; alternatives: Alternative[]; baseline: Alternative | null; delta_vs_baseline: Record<string, number> | null; evidence: string[]; uncertainty: string[]; engine_version: string };
type DecisionState = "accepted" | "modify" | "rejected" | null;
const pct = (value: number) => `${Math.round(value * 100)}%`;
const metric = (label: string, current: number | undefined, recommended: number, percentage = true) => ({ label, current: current ?? 0, recommended, percentage });
const managerEvidence: Record<Locale, Record<string, string>> = { uk: { "priority coverage": "Покриття пріоритетних потреб", "total coverage": "Загальне покриття потреб", "capacity utilization": "Використання доступної місткості команд", "travel cost": "Витрати на переміщення" }, en: { "priority coverage": "Priority needs covered", "total coverage": "Total needs covered", "capacity utilization": "Available team capacity used", "travel cost": "Travel cost" }, pl: { "priority coverage": "Pokrycie potrzeb priorytetowych", "total coverage": "Łączne pokrycie potrzeb", "capacity utilization": "Wykorzystanie dostępnej zdolności zespołów", "travel cost": "Koszt przejazdów" } };
function localizeEvidence(line: string, locale: Locale): string { const match = line.match(/^([^=]+)=(-?\d+(?:\.\d+)?)$/); if (!match) return line; const key = match[1].trim(); const value = Number(match[2]); const label = managerEvidence[locale][key] ?? key; if (["priority coverage", "total coverage", "capacity utilization"].includes(key)) return `${label}: ${pct(value)}`; if (key === "travel cost") return `${label}: ${value.toLocaleString(locale === "uk" ? "uk-UA" : locale === "pl" ? "pl-PL" : "en-US", { maximumFractionDigits: 2 })}`; return `${label}: ${value}`; }
export function ResourceAllocationWorkspace({ locale }: { locale: Locale }) { const [result,setResult]=useState<Result|null>(null); const [selected,setSelected]=useState(0); const [running,setRunning]=useState(false); const [error,setError]=useState<string|null>(null); const [decision,setDecision]=useState<DecisionState>(null);
async function run(){setRunning(true);setError(null);setDecision(null);try{const response=await fetch("/api/resource-allocation/run",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(DEMO)});const payload=await response.json();if(!response.ok)throw new Error(payload.error??"DIP request failed");setResult(payload);setSelected(0);}catch(e){setError(e instanceof Error?e.message:"DIP request failed");}finally{setRunning(false);}}
const active=result?.alternatives[selected]??result?.recommended; const metrics=active?[metric("Пріоритетні потреби",result?.baseline?.metrics.priority_coverage,active.metrics.priority_coverage),metric("Загальне покриття",result?.baseline?.metrics.total_coverage,active.metrics.total_coverage),metric("Невиконана потреба",result?.baseline?.metrics.unmet_need,active.metrics.unmet_need,false),metric("Використання ресурсу",result?.baseline?.metrics.capacity_utilization,active.metrics.capacity_utilization)]:[];
return <main className="min-h-screen bg-[#f7f5f2] text-[#191919]"><div className="mx-auto max-w-[1480px] px-5 py-8 md:px-10 lg:py-12"><header className="border-b border-black/15 pb-8"><div className="flex flex-wrap items-center justify-between gap-3"><div className="text-xs font-bold uppercase tracking-[.18em] text-[#d5222a]">DIP · Resource Allocation</div><div className="rounded-full border border-[#d5222a]/30 bg-white px-3 py-1 text-xs font-semibold text-[#b51e25]">Синтетичні демонстраційні дані</div></div><h1 className="mt-6 text-4xl font-black md:text-6xl">Розподіл мобільних команд</h1><p className="mt-5 max-w-3xl text-base leading-7 text-black/60">Який розподіл доступних команд дасть найбільше покриття пріоритетних потреб громад з урахуванням навичок, доступності, місткості та вартості переміщення?</p></header><section className="grid gap-6 py-8 lg:grid-cols-[.72fr_1.28fr]"><aside className="space-y-4"><div className="bg-white p-6"><div className="flex justify-between"><h2 className="text-xl font-bold">01 · Поточний стан</h2><button aria-label="Reset" onClick={()=>{setResult(null);setError(null);setDecision(null)}}><RotateCcw className="h-4 w-4"/></button></div><div className="mt-6 space-y-3">{DEMO.communities.map(c=><div key={c.id} className="border-t border-black/10 pt-3"><div className="flex justify-between"><b>{c.id}</b><span>{c.accessible?c.region:"Недоступна"}</span></div><div className="mt-2 text-sm text-black/55">{c.demand.map(d=>`${d.service}: ${d.units} · ${d.priority}`).join(" · ")}</div></div>)}</div></div><div className="bg-[#191919] p-6 text-white"><h2 className="text-xl font-bold">02 · Ресурс</h2><div className="mt-5 space-y-4">{DEMO.teams.map(t=><div key={t.id}><div className="flex justify-between"><b>{t.id}</b><span>capacity {t.capacity}</span></div><div>{t.skills.join(" · ")} · зараз: {t.current_community}</div></div>)}</div><button onClick={run} disabled={running} className="mt-7 flex w-full justify-center gap-2 bg-[#d5222a] p-4 font-bold"><Play className="h-4 w-4"/>{running?"DIP розраховує…":"Розрахувати розподіл"}</button></div></aside><div className="space-y-5">{error&&<div className="bg-white p-5 text-[#b51e25]"><CircleAlert className="inline h-5 w-5"/> {error}</div>}{!result&&!error&&<div className="flex min-h-[480px] items-center justify-center bg-white/60 text-center"><div><Users className="mx-auto h-10 w-10 text-[#d5222a]"/><h2 className="mt-5 text-2xl font-bold">03 · Рішення</h2><p>Запустіть DIP, щоб порівняти поточний розподіл з допустимими альтернативами.</p></div></div>}{result&&active&&<><div className="bg-white p-6"><div className="text-xs font-bold text-[#d5222a]">03 · РЕКОМЕНДОВАНИЙ РОЗПОДІЛ</div><h2 className="mt-2 text-3xl font-black">Покрити пріоритетний попит доступними спеціалістами</h2><div className="mt-7 grid gap-px sm:grid-cols-2 xl:grid-cols-4">{metrics.map(m=><div key={m.label} className="bg-[#faf9f7] p-4"><div>{m.label}</div><div className="text-2xl font-black">{m.percentage?pct(m.recommended):m.recommended.toFixed(0)}</div><div>було {m.percentage?pct(m.current):m.current.toFixed(0)} · Δ {m.percentage?`${Math.round((m.recommended-m.current)*100)} п.п.`:(m.recommended-m.current).toFixed(0)}</div></div>)}</div><div className="mt-7 grid gap-3 md:grid-cols-2">{Object.entries(active.assignments).map(([team,community])=><div key={team} className="border p-4"><div>{team}</div><div className="flex gap-2 font-bold"><span>{DEMO.teams.find(t=>t.id===team)?.current_community}</span><ArrowRight className="h-4 w-4"/><span>{community??"резерв"}</span></div></div>)}</div></div><div className="grid gap-5 xl:grid-cols-2"><div className="bg-white p-6"><h3 className="text-xl font-bold">04 · Альтернативи</h3>{result.alternatives.map((a,index)=><button key={index} onClick={()=>setSelected(index)} className="mt-2 w-full border p-4 text-left"><b>{index===0?"Рекомендовано":`Альтернатива ${index+1}`}</b><span className="float-right">{pct(a.metrics.priority_coverage)} пріоритетних потреб</span><div>загальне покриття {pct(a.metrics.total_coverage)} · витрати на переміщення {a.metrics.travel_cost.toFixed(0)}</div></button>)}</div><div className="bg-white p-6"><h3 className="text-xl font-bold">05 · Чому цей розподіл?</h3><div className="mt-4 space-y-3">{active.evidence.map(line=><div key={line} className="flex gap-2"><ArrowRight className="h-4 w-4 text-[#d5222a]"/>{localizeEvidence(line,locale)}</div>)}{result.uncertainty.map(line=><div key={line} className="flex gap-2 text-[#a86b00]"><CircleAlert className="h-4 w-4"/>{line}</div>)}</div></div></div><div className="flex flex-wrap gap-3 border-t pt-5"><span className="mr-auto">Рішення підтверджує менеджер · демонстраційний режим</span><button onClick={()=>setDecision("rejected")}><X className="inline h-4 w-4"/> Відхилити</button><button onClick={()=>setDecision("modify")}>Змінити</button><button onClick={()=>setDecision("accepted")} className="bg-[#d5222a] px-5 py-3 text-white"><Check className="inline h-4 w-4"/> Прийняти</button>{decision&&<span>Стан рішення: {decision}</span>}</div></>}</div></section></div></main>;
}
